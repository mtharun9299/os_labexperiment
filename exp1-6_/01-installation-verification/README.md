# Experiment 1 – Installation of Windows / Kali Linux

The supplied experiment contains installation steps and verification commands rather than runnable code.

## Windows verification
```text
winver
systeminfo
hostname
ipconfig
```

## Kali Linux verification
```bash
uname -a
cat /etc/os-release
whoami
hostname
lscpu
df -h
```
